name: Deploy via SSH
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Deploy via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ssh.eu.pythonanywhere.com
        username: ErikGoranssonGaspar
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /home/ErikGoranssonGaspar
          if [ ! -d "goranssongaspar.com" ]; then
            git clone https://github.com/ErikGoranssonGaspar/goranssongaspar.com.git
          fi
          cd goranssongaspar.com
          git pull origin main
          echo "Deployment completed successfully"